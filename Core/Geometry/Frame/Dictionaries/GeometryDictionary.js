/* Границы геометрии кадра. Только константы, без методов и состояния. */

class GeometryDictionary {
  static MIN_SIDE = 16;
  static MAX_SIDE = 30000;
  static MIN_ZOOM = 0.05;
  static MAX_ZOOM = 4;
  static FILL_ZOOM = 1;          // 1 — картинка заполняет холст целиком
}
